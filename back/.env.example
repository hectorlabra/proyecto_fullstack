# ===========================================
# CONFIGURACIÓN DEL SERVIDOR
# ===========================================

# Puerto en el que correrá el servidor Express
# Desarrollo: 3000 | Producción: Asignado por Render/Heroku
PORT=3000

# Versión de la aplicación (semantic versioning)
# Se muestra en endpoint /version
APP_VERSION=1.0.0

# ===========================================
# CONFIGURACIÓN DE BASE DE DATOS POSTGRESQL
# ===========================================

# OPCIÓN 1: URL completa de conexión (Recomendado para Render/Heroku)
# Si DATABASE_URL existe, tiene prioridad sobre las variables individuales
# Formato: postgresql://username:password@host:port/database
# Render proporciona esto automáticamente en la sección "Internal Database URL"
# DATABASE_URL=postgresql://user:password@host:5432/database

# OPCIÓN 2: Variables individuales (Desarrollo local)
# Usar estas variables si no tienes DATABASE_URL

# Host del servidor PostgreSQL
# Desarrollo: localhost | Producción: URL de Render/Heroku
DB_HOST=localhost

# Puerto de PostgreSQL (por defecto 5432)
DB_PORT=5432

# Usuario de PostgreSQL
DB_USERNAME=postgres

# Contraseña del usuario de PostgreSQL
# ⚠️ CAMBIAR en producción - Usar valores secretos de Render/Vercel
DB_PASSWORD=your_secure_password_here

# Nombre de la base de datos
DB_DATABASE=medical_appointments

# Habilitar SSL para conexión a base de datos
# Desarrollo: false | Producción: true (requerido por Render)
# Nota: Si usas DATABASE_URL, SSL se habilita automáticamente
DB_SSL=false

# ===========================================
# CONFIGURACIÓN DE ENTORNO
# ===========================================

# Entorno de ejecución: development | production | test
# Controla: logging, synchronize de TypeORM, mensajes de error
NODE_ENV=development

# ===========================================
# CONFIGURACIÓN DE CORS
# ===========================================

# Lista de orígenes permitidos para CORS (separados por comas)
# Desarrollo: http://localhost:5173,http://localhost:3000
# Producción: https://tu-frontend.onrender.com,https://tu-dominio.com
ALLOWED_ORIGINS=http://localhost:5173

# ===========================================
# CONFIGURACIÓN DE RATE LIMITING
# ===========================================

# Habilitar limitación de tasa (100 requests/15min)
# true: activo (recomendado en producción)
# false: desactivado (solo para desarrollo/testing)
ENABLE_RATE_LIMIT=true
